import brand from '../schemas/brand'
import catalog from '../schemas/catalog'
import featured from '../schemas/featured'
import product from '../schemas/product'

export const schemaTypes = [product, catalog, brand, featured]
